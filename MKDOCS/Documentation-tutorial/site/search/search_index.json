{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Home","text":"<p>For full documentation visit mkdocs.org.</p>"},{"location":"#commands","title":"Commands","text":"<ul> <li><code>mkdocs new [dir-name]</code> - Create a new project.</li> <li><code>mkdocs serve</code> - Start the live-reloading docs server.</li> <li><code>mkdocs build</code> - Build the documentation site.</li> <li><code>mkdocs -h</code> - Print help message and exit.</li> </ul>"},{"location":"#project-layout","title":"Project layout","text":"Text Only<pre><code>mkdocs.yml    # The configuration file.\ndocs/\n    index.md  # The documentation homepage.\n    ...       # Other markdown pages, images and other files.\n</code></pre> <p>This is my site and i am the this code </p>"},{"location":"#content-tabs","title":"Content tabs","text":"ChimayViewEdit <p>This is good.</p> <p>This is the fact</p> <p>You Cant edit it</p>"},{"location":"#admonition","title":"Admonition","text":"<p>Note</p> <p>Example of the note</p> <p>Warning</p> <p>This is the warning </p> <p>Danger</p> <p>You are in danger </p> <p>Example</p> <p>This is the Example</p> <p>This is the tip</p> <p>This is the Tip </p>"},{"location":"Flowchart/","title":"Flowchart","text":""},{"location":"Flowchart/#flowchart-examples-using-mermaid","title":"Flowchart Examples using mermaid","text":"<pre><code>graph LR\n  A[Start] --&gt; B{Error?};\n  B --&gt;|Yes| C[Hmm...];\n  C --&gt; D[Debug];\n  D --&gt; B;\n  B ----&gt;|No| E[Yay!];</code></pre> <pre><code>sequenceDiagram\n  autonumber\n  Alice-&gt;&gt;John: Hello John, how are you?\n  loop Healthcheck\n      John-&gt;&gt;John: Fight against hypochondria\n  end\n  Note right of John: Rational thoughts!\n  John--&gt;&gt;Alice: Great!\n  John-&gt;&gt;Bob: How about you?\n  Bob--&gt;&gt;John: Jolly good!</code></pre> <pre><code>stateDiagram-v2\n  state fork_state &lt;&lt;fork&gt;&gt;\n    [*] --&gt; fork_state\n    fork_state --&gt; State2\n    fork_state --&gt; State3\n\n    state join_state &lt;&lt;join&gt;&gt;\n    State2 --&gt; join_state\n    State3 --&gt; join_state\n    join_state --&gt; State4\n    State4 --&gt; [*]</code></pre> <pre><code>classDiagram\n  Person &lt;|-- Student\n  Person &lt;|-- Professor\n  Person : +String name\n  Person : +String phoneNumber\n  Person : +String emailAddress\n  Person: +purchaseParkingPass()\n  Address \"1\" &lt;-- \"0..1\" Person:lives at\n  class Student{\n    +int studentNumber\n    +int averageMark\n    +isEligibleToEnrol()\n    +getSeminarsTaken()\n  }\n  class Professor{\n    +int salary\n  }\n  class Address{\n    +String street\n    +String city\n    +String state\n    +int postalCode\n    +String country\n    -validate()\n    +outputAsLabel()\n  }</code></pre> <pre><code>erDiagram\n  CUSTOMER ||--o{ ORDER : places\n  ORDER ||--|{ LINE-ITEM : contains\n  LINE-ITEM {\n    string name\n    int pricePerUnit\n  }\n  CUSTOMER }|..|{ DELIVERY-ADDRESS : uses</code></pre>"},{"location":"Footnotes/","title":"Footnote","text":""},{"location":"Footnotes/#foot-notes","title":"Foot Notes","text":"<p>Lorem ipsum<sup>1</sup> dolor sit amet, consectetur adipiscing elit.<sup>2</sup></p> <ol> <li> <p>Lorem ipsum dolor sit amet, consectetur adipiscing elit.\u00a0\u21a9</p> </li> <li> <p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Nulla et euismod nulla. Curabitur feugiat, tortor non consequat finibus, justo purus auctor massa, nec semper lorem quam in massa.\u00a0\u21a9</p> </li> </ol>"},{"location":"Grid/","title":"Grid","text":""},{"location":"Grid/#grid","title":"Grid","text":"<ul> <li> HTML for content and structure</li> <li> JavaScript for interactivity</li> <li> CSS for text running out of boxes</li> <li> Internet Explorer ... huh?</li> </ul>"},{"location":"Type/","title":"Type","text":"Data Type Size in Bytes int 4 bytes double 8 bytes <p>D<sub>a</sub>ta t<sup>a</sup>ble</p> <p></p>"},{"location":"about/","title":"About","text":"<p>For full documentation visit mkdocs.org.</p>"},{"location":"about/#commands","title":"Commands","text":"<ul> <li><code>mkdocs new [dir-name]</code> - Create a new project.</li> <li><code>mkdocs serve</code> - Start the live-reloading docs server.</li> <li><code>mkdocs build</code> - Build the documentation site.</li> <li><code>mkdocs -h</code> - Print help message and exit.</li> </ul>"},{"location":"about/#project-layout","title":"Project layout","text":"Text Only<pre><code>mkdocs.yml    # The configuration file.\ndocs/\n    index.md  # The documentation homepage.\n    ...       # Other markdown pages, images and other files.\n</code></pre>"},{"location":"api/","title":"API Documentation","text":""},{"location":"code/","title":"Code","text":"<p>An example of a codeblock for Python:</p>"},{"location":"code/#python-code","title":"Python Code","text":"Python<pre><code># function to add two numbers\n\ndef add_two_numbers(num1,num2):\n    # jkhsjkhshj \n    return num1 + num2\n\n#Example usage\n\nresult = add_two_numbers(5,3)\n# res  = add_two_numbers(3,5) \nprint(\"The sum is : \", result)\n</code></pre>"},{"location":"code/#my-project","title":"My Project","text":""},{"location":"code/#reference-page","title":"Reference Page","text":""},{"location":"code/#main.BBoxSchema","title":"<code>BBoxSchema</code>","text":"<p>               Bases: <code>Struct</code></p> <p>Schema Representing OCR data with Bounding Box. </p> <p>This class is used to store OCR results in  Numpy Array.</p> <p>Attributes:</p> Name Type Description <code>parent_ids</code> <code>list[int]</code> <p>parent id for the OCR blocks. </p> <code>texts</code> <code>list[str]</code> <p>Extracted text content.</p> <code>bboxes</code> <code>list[BBox]</code> <p>Bounding box for the text.</p> Source code in <code>main.py</code> Python<pre><code>class BBoxSchema(Struct):\n    \"\"\"\n    Schema Representing OCR data with Bounding Box. \n\n    This class is used to store OCR results in  Numpy Array.\n\n    Attributes:\n        parent_ids (list[int]): parent id for the OCR blocks. \n        texts (list[str]): Extracted text content.\n        bboxes (list[BBox]): Bounding box for the text.\n    \"\"\"\n    parent_ids: list[int] = field(name=\"parentIDs\")\n    texts: list[str]\n    bboxes: list[BBox]\n\n    def to_python(self) -&gt; OcrBlockCollection:\n        \"\"\"\n        Convert the BBox Schema data into OcrBlockCollections.\n\n        This method converts the data into into numpy arrays and returns them into the OcrBlock Collection.\n\n        Returns :\n            OcrBlockCollection: Collection containing parent_ids, texts  and bounding boxes as numpy arrays.\n        \"\"\"\n        return OcrBlockCollection(\n            parent_ids=np.array(self.parent_ids, dtype=int),\n            texts=np.array(self.texts, dtype=str),\n            bboxes=np.array(self.bboxes, dtype=int),\n        )\n</code></pre>"},{"location":"code/#main.BBoxSchema.to_python","title":"<code>to_python()</code>","text":"<p>Convert the BBox Schema data into OcrBlockCollections.</p> <p>This method converts the data into into numpy arrays and returns them into the OcrBlock Collection.</p> Returns <p>OcrBlockCollection: Collection containing parent_ids, texts  and bounding boxes as numpy arrays.</p> Source code in <code>main.py</code> Python<pre><code>def to_python(self) -&gt; OcrBlockCollection:\n    \"\"\"\n    Convert the BBox Schema data into OcrBlockCollections.\n\n    This method converts the data into into numpy arrays and returns them into the OcrBlock Collection.\n\n    Returns :\n        OcrBlockCollection: Collection containing parent_ids, texts  and bounding boxes as numpy arrays.\n    \"\"\"\n    return OcrBlockCollection(\n        parent_ids=np.array(self.parent_ids, dtype=int),\n        texts=np.array(self.texts, dtype=str),\n        bboxes=np.array(self.bboxes, dtype=int),\n    )\n</code></pre>"},{"location":"code/#main.LineCollection","title":"<code>LineCollection</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TextBlockCollection</code></p> <p>Represents a collection of Lines. </p> <p>This class provides operations for line collections, extending TextBlockCollection . It returns Line Objects for single elements  and suports indexing, slicing and aggregation at the Line</p> Attributes <p>parent_id (IntVector): Parent id for the text block. texts (StrVector): Array of text vector. bboxes (Array2D): Bounding Box for text element in format [x1,y1,x2,y2].</p> Source code in <code>main.py</code> Python<pre><code>class LineCollection(TextBlockCollection):\n    \"\"\"\n    Represents a collection of Lines. \n\n    This class provides operations for line collections,\n    extending TextBlockCollection . It returns Line Objects for single elements \n    and suports indexing, slicing and aggregation at the Line\n\n    Attributes :\n        parent_id (IntVector): Parent id for the text block.\n        texts (StrVector): Array of text vector.\n        bboxes (Array2D): Bounding Box for text element in format [x1,y1,x2,y2].\n    \"\"\"\n    parent_ids: IntVector\n    texts: StrVector\n    bboxes: Array2D[Shape[int, Literal[4]], np.int_]\n\n    def item(self) -&gt; Line:\n        \"\"\"\n        Return a single Line as a line Object.\n\n        Returns:\n            Line: Single Line element. \n\n        Raises:\n            ValueError: if collection contains more than one element.\n        \"\"\"\n        return Line(super().item())\n\n    def as_block(self) -&gt; TextBlock:\n        \"\"\"\n        Combine all lines into a single TextBlock.\n\n        Merges all line text using line seperators along with  bounding box computations.\n\n        Returns: \n            TextBlock: Aggregated line block.\n        \"\"\"\n        bbox_min = self.bboxes.min(axis=0)\n        bbox_max = self.bboxes.max(axis=0)\n        bbox = np.r_[bbox_min[:2], bbox_max[2:]]\n        return TextBlock(text=LINE_SEP.join(self.texts), bbox=bbox)\n\n    def __len__(self) -&gt; int:\n        \"\"\"\n        Returns the number of lines in the collections.\n\n        Returns:\n            int: Total number of lines.\n\n        \"\"\"\n        return super().count()\n\n    def count(self) -&gt; int:\n        \"\"\"\n        Returns the total nuber of lines.\n\n        Returns:\n            int: Total number of lines.\n        \"\"\"\n        return super().count()\n\n    def first(self) -&gt; Line:\n        \"\"\"\n        Returns the first line from Collection.\n\n        Returns:\n            Line: first line element.\n        \"\"\"\n        return Line(super().first())\n\n    def last(self) -&gt; Line:\n        \"\"\"\n        Returns the last line from Collection.\n\n        Returns:\n            Line: last line element.\n        \"\"\"\n        return Line(super().last())\n\n    def at(self, index: int) -&gt; Line:\n        \"\"\"\n        Returns the line at a specified index.\n\n        Args:\n            index (int): Position of the line.\n\n        Returns: \n            Line: line at specified index.  \n\n        \"\"\"\n        return Line(super().at(index))\n\n    @overload\n    def __getitem__(self, index: int) -&gt; Line: ...\n\n    @overload\n    def __getitem__(self, index: MultiIndexer | None) -&gt; LineCollection: ...\n\n    def __getitem__(self, index: Indexer) -&gt; LineCollection | Line:\n        \"\"\"\n        Retrives line by indexing\n\n        if index is int  then\n            Returns: Line object.\n        else (for multiIndexer)\n            Returns: LineCollection. \n\n        Args: \n            index (Indexer):  Index or Indexing sequence.\n\n        Returns: \n            Line or LineCollection:  Based on index type. \n        \"\"\"\n        if isinstance(index, int):\n            return Line(\n                OcrBlock(\n                    parent_id=self.parent_ids[index],\n                    text=self.texts[index],\n                    bbox=self.bboxes[index],\n                )\n            )\n        return LineCollection(\n            parent_ids=self.parent_ids[index],\n            texts=self.texts[index],\n            bboxes=self.bboxes[index],\n        )\n</code></pre>"},{"location":"code/#main.LineCollection.__getitem__","title":"<code>__getitem__(index)</code>","text":"Python<pre><code>__getitem__(index: int) -&gt; Line\n</code></pre>Python<pre><code>__getitem__(index: MultiIndexer | None) -&gt; LineCollection\n</code></pre> <p>Retrives line by indexing</p> <p>if index is int  then     Returns: Line object. else (for multiIndexer)     Returns: LineCollection. </p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>Indexer</code> <p>Index or Indexing sequence.</p> required <p>Returns:</p> Type Description <code>LineCollection | Line</code> <p>Line or LineCollection:  Based on index type.</p> Source code in <code>main.py</code> Python<pre><code>def __getitem__(self, index: Indexer) -&gt; LineCollection | Line:\n    \"\"\"\n    Retrives line by indexing\n\n    if index is int  then\n        Returns: Line object.\n    else (for multiIndexer)\n        Returns: LineCollection. \n\n    Args: \n        index (Indexer):  Index or Indexing sequence.\n\n    Returns: \n        Line or LineCollection:  Based on index type. \n    \"\"\"\n    if isinstance(index, int):\n        return Line(\n            OcrBlock(\n                parent_id=self.parent_ids[index],\n                text=self.texts[index],\n                bbox=self.bboxes[index],\n            )\n        )\n    return LineCollection(\n        parent_ids=self.parent_ids[index],\n        texts=self.texts[index],\n        bboxes=self.bboxes[index],\n    )\n</code></pre>"},{"location":"code/#main.LineCollection.__len__","title":"<code>__len__()</code>","text":"<p>Returns the number of lines in the collections.</p> <p>Returns:</p> Name Type Description <code>int</code> <code>int</code> <p>Total number of lines.</p> Source code in <code>main.py</code> Python<pre><code>def __len__(self) -&gt; int:\n    \"\"\"\n    Returns the number of lines in the collections.\n\n    Returns:\n        int: Total number of lines.\n\n    \"\"\"\n    return super().count()\n</code></pre>"},{"location":"code/#main.LineCollection.as_block","title":"<code>as_block()</code>","text":"<p>Combine all lines into a single TextBlock.</p> <p>Merges all line text using line seperators along with  bounding box computations.</p> <p>Returns:</p> Name Type Description <code>TextBlock</code> <code>TextBlock</code> <p>Aggregated line block.</p> Source code in <code>main.py</code> Python<pre><code>def as_block(self) -&gt; TextBlock:\n    \"\"\"\n    Combine all lines into a single TextBlock.\n\n    Merges all line text using line seperators along with  bounding box computations.\n\n    Returns: \n        TextBlock: Aggregated line block.\n    \"\"\"\n    bbox_min = self.bboxes.min(axis=0)\n    bbox_max = self.bboxes.max(axis=0)\n    bbox = np.r_[bbox_min[:2], bbox_max[2:]]\n    return TextBlock(text=LINE_SEP.join(self.texts), bbox=bbox)\n</code></pre>"},{"location":"code/#main.LineCollection.at","title":"<code>at(index)</code>","text":"<p>Returns the line at a specified index.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>int</code> <p>Position of the line.</p> required <p>Returns:</p> Name Type Description <code>Line</code> <code>Line</code> <p>line at specified index.</p> Source code in <code>main.py</code> Python<pre><code>def at(self, index: int) -&gt; Line:\n    \"\"\"\n    Returns the line at a specified index.\n\n    Args:\n        index (int): Position of the line.\n\n    Returns: \n        Line: line at specified index.  \n\n    \"\"\"\n    return Line(super().at(index))\n</code></pre>"},{"location":"code/#main.LineCollection.count","title":"<code>count()</code>","text":"<p>Returns the total nuber of lines.</p> <p>Returns:</p> Name Type Description <code>int</code> <code>int</code> <p>Total number of lines.</p> Source code in <code>main.py</code> Python<pre><code>def count(self) -&gt; int:\n    \"\"\"\n    Returns the total nuber of lines.\n\n    Returns:\n        int: Total number of lines.\n    \"\"\"\n    return super().count()\n</code></pre>"},{"location":"code/#main.LineCollection.first","title":"<code>first()</code>","text":"<p>Returns the first line from Collection.</p> <p>Returns:</p> Name Type Description <code>Line</code> <code>Line</code> <p>first line element.</p> Source code in <code>main.py</code> Python<pre><code>def first(self) -&gt; Line:\n    \"\"\"\n    Returns the first line from Collection.\n\n    Returns:\n        Line: first line element.\n    \"\"\"\n    return Line(super().first())\n</code></pre>"},{"location":"code/#main.LineCollection.item","title":"<code>item()</code>","text":"<p>Return a single Line as a line Object.</p> <p>Returns:</p> Name Type Description <code>Line</code> <code>Line</code> <p>Single Line element. </p> <p>Raises:</p> Type Description <code>ValueError</code> <p>if collection contains more than one element.</p> Source code in <code>main.py</code> Python<pre><code>def item(self) -&gt; Line:\n    \"\"\"\n    Return a single Line as a line Object.\n\n    Returns:\n        Line: Single Line element. \n\n    Raises:\n        ValueError: if collection contains more than one element.\n    \"\"\"\n    return Line(super().item())\n</code></pre>"},{"location":"code/#main.LineCollection.last","title":"<code>last()</code>","text":"<p>Returns the last line from Collection.</p> <p>Returns:</p> Name Type Description <code>Line</code> <code>Line</code> <p>last line element.</p> Source code in <code>main.py</code> Python<pre><code>def last(self) -&gt; Line:\n    \"\"\"\n    Returns the last line from Collection.\n\n    Returns:\n        Line: last line element.\n    \"\"\"\n    return Line(super().last())\n</code></pre>"},{"location":"code/#main.OcrBlock","title":"<code>OcrBlock</code>","text":"<p>               Bases: <code>NamedTuple</code></p> <p>Represents a single OCR block.</p> <p>This class stores information for one detected text element ,including its parent relationship ,text content and bounding box</p> Attributes <p>parent_id (int): parent id for OCR block text (str): Extracted text content  bbox (Vector[Shape[Literal[4]], np.int_]): bounding box coordinates in format [x1,y1,x2,y2].</p> Source code in <code>main.py</code> Python<pre><code>class OcrBlock(NamedTuple):\n    \"\"\"\n        Represents a single OCR block.\n\n        This class stores information for one detected text element ,including its parent relationship ,text content and bounding box\n\n        Attributes :\n            parent_id (int): parent id for OCR block\n            text (str): Extracted text content \n            bbox (Vector[Shape[Literal[4]], np.int_]): bounding box coordinates in format [x1,y1,x2,y2].\n\n    \"\"\"\n    parent_id: int\n    text: str\n    bbox: Vector[Shape[Literal[4]], np.int_]\n\n    def as_textbox(self) -&gt; TextBlock:\n        \"\"\"\n            Converts OCR data into Textblock. \n\n            Returns: \n                TextBlock: Object containing text and bounding box coordinates.  \n        \"\"\"\n        return TextBlock(text=self.text, bbox=self.bbox)\n</code></pre>"},{"location":"code/#main.OcrBlock.as_textbox","title":"<code>as_textbox()</code>","text":"<p>Converts OCR data into Textblock. </p> <p>Returns:</p> Name Type Description <code>TextBlock</code> <code>TextBlock</code> <p>Object containing text and bounding box coordinates.</p> Source code in <code>main.py</code> Python<pre><code>def as_textbox(self) -&gt; TextBlock:\n    \"\"\"\n        Converts OCR data into Textblock. \n\n        Returns: \n            TextBlock: Object containing text and bounding box coordinates.  \n    \"\"\"\n    return TextBlock(text=self.text, bbox=self.bbox)\n</code></pre>"},{"location":"code/#main.OcrBlockCollection","title":"<code>OcrBlockCollection</code>  <code>dataclass</code>","text":"<p>               Bases: <code>Sized</code></p> <p>Represents a collection of OCR blocks for paragraph,lines and words.</p> <p>This class stores the OCR  results  in a structured  format using Numpy Arrays.</p> <p>Attributes:</p> Name Type Description <code>parent_ids</code> <code>IntVector</code> <p>Parent id for the ocr block.</p> <code>texts</code> <code>StrVector</code> <p>Extracted Text content. </p> <code>bboxes</code> <code>Array2D</code> <p>Bounding box for the text.</p> Source code in <code>main.py</code> Python<pre><code>@dataclass\nclass OcrBlockCollection(Sized):\n    \"\"\"\n    Represents a collection of OCR blocks for paragraph,lines and words.\n\n    This class stores the OCR  results  in a structured  format using Numpy Arrays.\n\n    Attributes:\n        parent_ids (IntVector): Parent id for the ocr block.\n        texts (StrVector): Extracted Text content. \n        bboxes (Array2D): Bounding box for the text.\n\n    \"\"\"\n    parent_ids: IntVector\n    texts: StrVector\n    bboxes: Array2D[Shape[int, Literal[4]], np.int_]\n\n    def __post_init__(self) -&gt; None:\n        \"\"\"\n        Initializes indices for paragraph , lines , words \n\n        Splits OCR blocks into :\n        - Paragraph ids\n        - Line ids\n        - word ids\n\n        \"\"\"\n        self.para_ids = np.flatnonzero(self.parent_ids == -1)\n        self.line_ids = np.flatnonzero(\n            (self.parent_ids &gt;= 0) &amp; (self.parent_ids &lt; self.para_ids.size)\n        )\n        self.word_ids = np.flatnonzero(\n            (self.parent_ids &gt;= self.para_ids.size)\n            &amp; (self.parent_ids &lt; (self.para_ids.size + self.line_ids.size))\n        )\n\n    def __len__(self) -&gt; int:\n        \"\"\"\n            Returns the number of items.\n        \"\"\"\n        return self.count()\n\n    def count(self) -&gt; int:\n        \"\"\"\n            Returns the calculated size of the text content. \n        \"\"\"\n        return self.texts.size\n\n    @overload\n    def __getitem__(\n        self,\n        levels: int\n        | MultiIndexer\n        | tuple[int | MultiIndexer]\n        | tuple[int | MultiIndexer, None]\n        | tuple[int | MultiIndexer, None, None],\n    ) -&gt; ParaCollection: ...\n    @overload\n    def __getitem__(\n        self,\n        levels: tuple[Indexer, int | MultiIndexer]\n        | tuple[Indexer, int | MultiIndexer, None],\n    ) -&gt; LineCollection: ...\n    @overload\n    def __getitem__(\n        self,\n        levels: tuple[Indexer, Indexer, int | MultiIndexer]\n        | tuple[None]\n        | tuple[None, None]\n        | tuple[None, None, None],\n    ) -&gt; WordCollection: ...\n    @overload\n    def __getitem__(\n        self, levels: tuple[Indexer, Indexer, Indexer]\n    ) -&gt; TextBlockCollection: ...\n    def __getitem__(\n        self,\n        levels: int\n        | MultiIndexer\n        | tuple[Indexer]\n        | tuple[Indexer, Indexer]\n        | tuple[Indexer, Indexer, Indexer],paragraph\n    ) -&gt; TextBlockCollection:\n        \"\"\"\n            Generates OCR blocks\n\n            indexing is in form:\n                object[para(0)],\n                object[para(0),line(1)],\n                object[para(0),line(1),word(2)],\n\n            Returns:\n                WordCollection, LineCollection, ParaCollection depending on the indexing.\n        \"\"\"\n        match levels:\n            case (p,) | int(p) | (slice(start=_, stop=_, step=_) as p):\n                para, line, word = p, None, None\n            case (p, l):\n                para, line, word = p, l, None\n            case (p, l, w):\n                para, line, word = p, l, w\n            case _:\n                raise IndexError(f\"Too many indexers. Expected=3, got={len(levels)}\")\n\n        para_indexer, line_indexer, word_indexer = map(\n            _get_multiindexer, (para, line, word)\n        )\n        para_ids_picked = self.para_ids[para_indexer]\n        line_ids_picked = self.line_ids[line_indexer]\n        word_ids_picked = self.word_ids[word_indexer]\n\n        fast_path_possible = _is_contiguous_slice(\n            para_indexer, line_indexer, word_indexer\n        )\n        match (para, line, word):\n            case (None, None, _):\n                return WordCollection.from_ocr(\n                    self.pick_indices(self.word_ids[word_indexer])\n                )\n            case (None, _, None):\n                return LineCollection.from_ocr(\n                    self.pick_indices(self.line_ids[line_indexer])\n                )\n            case (_, None, None):\n                return ParaCollection.from_ocr(\n                    self.pick_indices(self.para_ids[para_indexer])\n                )\n            case (None, _, _):\n                if fast_path_possible:\n                    word_parent_ids = self.parent_ids[word_ids_picked]\n                    in_range_mask = ((word_parent_ids &gt;= line_ids_picked[0])\n                        &amp; (word_parent_ids &lt; line_ids_picked[-1] + 1))  # fmt: skip\n                    return WordCollection(\n                        parent_ids=word_parent_ids[in_range_mask],\n                        texts=self.texts[word_ids_picked][in_range_mask],\n                        bboxes=self.bboxes[word_ids_picked][in_range_mask],\n                    )\n                unique_line_ids = np.unique(line_ids_picked)\n                relevant_ids = np.flatnonzero(np.isin(self.parent_ids, unique_line_ids))\n                return WordCollection.from_ocr(\n                    self.pick_indices(relevant_ids[word_indexer])\n                )\n            case (_, _, None):\n                if fast_path_possible:\n                    line_parent_ids = self.parent_ids[line_ids_picked]\n                    in_range_mask = ((line_parent_ids &gt;= para_ids_picked[0])\n                        &amp; (line_parent_ids &lt; para_ids_picked[-1] + 1))  # fmt: skip\n                    return LineCollection(\n                        parent_ids=line_parent_ids[in_range_mask],\n                        texts=self.texts[line_ids_picked][in_range_mask],\n                        bboxes=self.bboxes[line_ids_picked][in_range_mask],\n                    )\n                unique_para_ids = np.unique(para_ids_picked)\n                relevant_ids = np.flatnonzero(np.isin(self.parent_ids, unique_para_ids))\n                return LineCollection.from_ocr(\n                    self.pick_indices(relevant_ids[line_indexer])\n                )\n            case _:\n                if fast_path_possible:\n                    line_parent_ids = self.parent_ids[line_ids_picked]\n                    relevant_line_ids = line_ids_picked[\n                        (line_parent_ids &gt;= para_ids_picked[0])\n                        &amp; (line_parent_ids &lt; para_ids_picked[-1] + 1)\n                    ]\n                    word_parent_ids = self.parent_ids[word_ids_picked]\n                    in_range_mask = (word_parent_ids &gt;= relevant_line_ids[0]) &amp; (\n                        word_parent_ids &lt; relevant_line_ids[-1] + 1\n                    )\n                    return WordCollection(\n                        parent_ids=word_parent_ids[in_range_mask],\n                        texts=self.texts[word_ids_picked][in_range_mask],\n                        bboxes=self.bboxes[word_ids_picked][in_range_mask],\n                    )\n                unique_para_ids = np.unique(para_ids_picked)\n                relevant_line_ids = np.flatnonzero(\n                    np.isin(self.parent_ids, unique_para_ids)\n                )\n                unique_line_ids = np.unique(relevant_line_ids[line_indexer])\n                relevant_ids = np.flatnonzero(np.isin(self.parent_ids, unique_line_ids))\n                return WordCollection.from_ocr(\n                    self.pick_indices(relevant_ids[word_indexer])\n                )\n\n    # def get_level(\n    #     self,\n    #     *,\n    #     para: Indexer = None,\n    #     line: Indexer = None,\n    #     word: Indexer = None,\n    # ) -&gt; TextBlockCollection:\n    #     return self[para, line, word]\n\n    def pick_indices(\n        self,\n        indexer: MultiIndexer,\n    ) -&gt; OcrBlockCollection:\n        \"\"\"\n            Returns OcrBlockCollection with the selected indices\n\n            This Method filters the current collection based on the provided indexer and returns a new object with corresponding parent_id , texts and bboxes \n\n            Args:\n                indexer (MultiIndexer): Index , Slice , List , Boolean vector to select element from collections.\n\n            Returns:\n                OcrBlockCollection: Collection of the filtered items. \n\n            Raises:\n                TypeError: If the indexer is None.   \n        \"\"\"\n        if indexer is None:\n            raise TypeError(f\"{type(indexer)} is not a valid indexer type\")\n        return OcrBlockCollection(\n            parent_ids=self.parent_ids[indexer],\n            texts=self.texts[indexer],\n            bboxes=self.bboxes[indexer],\n        )\n\n    def item(self) -&gt; OcrBlock:\n        \"\"\"\n            Returns Single OcrBlock from Collection.\n\n            This Method checks whether the collection contains only one elemenet or not \n            ifmore than one is present then it raises error.  \n\n            Returns:\n                OcrBlock: The Single OCR Block in the collection. \n\n            Raises:\n                ValueError: If the collection contains more than one element.   \n        \"\"\"\n        if self.parent_ids.size != 1:\n            raise ValueError(\"Too many items in OcrResult\")\n        return OcrBlock(\n            parent_id=self.parent_ids[0],\n            text=self.texts[0],\n            bbox=self.bboxes[0],\n        )\n\n    def first(self) -&gt; OcrBlock:\n        \"\"\"\n        Returns the first OCR blcok In collection.\n        \"\"\"\n        return self.at(0)\n\n    def last(self) -&gt; OcrBlock:\n        \"\"\"\n        Returns the Last OCR blcok In collection.\n        \"\"\"\n        return self.at(-1)\n\n    def at(self, index: int) -&gt; OcrBlock:\n        \"\"\"\n        Returns Ocr Block at specific index. \n\n        Args:\n            index (int): Position of the element.\n\n        Returns:\n            OcrBlock: The selected Ocr Block.\n        \"\"\"\n        return self.pick_indices([index]).item()\n\n    def lines_by_para(\n        self, para_indexer: MultiIndexer | None = None\n    ) -&gt; Iterator[LineCollection]:\n        \"\"\"\n        Yield lines from paragraph \n\n        Args:\n            para_indexer (MultiIndexer,optional): index to filter paragraph. \n\n        Yields:\n            Collection of Lines from the paragraph. \n        \"\"\"\n        match para_indexer:\n            case None:\n                yield from (self[i, slice(None)] for i in self.para_ids)\n            case _:\n                yield from (self[i, slice(None)] for i in self.para_ids[para_indexer])\n\n    def words_by_para(\n        self, para_indexer: MultiIndexer | None = None\n    ) -&gt; Iterator[WordCollection]:\n        \"\"\"\n        Yield words from paragraph \n\n        Args:\n            para_indexer (MultiIndexer,optional): index to filter paragraph.\n\n        Yields:\n            Collection of words from the paragraph.\n\n        \"\"\"    \n        match para_indexer:\n            case None:\n                yield from (self[i, None, slice(None)] for i in self.para_ids)\n            case _:\n                yield from (\n                    self[i, None, slice(None)] for i in self.para_ids[para_indexer]\n                )\n\n    def words_by_line(\n        self, line_indexer: MultiIndexer | None = None\n    ) -&gt; Iterator[WordCollection]:\n        \"\"\"\n        Yield words from lines.\n\n        Args:\n            line_indexer (MultiIndexer,optional): index to filter line. \n\n        Yields:\n            Collection of words from line.\n        \"\"\"\n        match line_indexer:\n            case None:\n                yield from (self[i, None, slice(None)] for i in self.para_ids)\n            case _:\n                yield from (\n                    self[None, i, slice(None)] for i in self.line_ids[line_indexer]\n                )\n</code></pre>"},{"location":"code/#main.OcrBlockCollection.__getitem__","title":"<code>__getitem__(levels, paragraph)</code>","text":"Python<pre><code>__getitem__(levels: int | MultiIndexer | tuple[int | MultiIndexer] | tuple[int | MultiIndexer, None] | tuple[int | MultiIndexer, None, None]) -&gt; ParaCollection\n</code></pre>Python<pre><code>__getitem__(levels: tuple[Indexer, int | MultiIndexer] | tuple[Indexer, int | MultiIndexer, None]) -&gt; LineCollection\n</code></pre>Python<pre><code>__getitem__(levels: tuple[Indexer, Indexer, int | MultiIndexer] | tuple[None] | tuple[None, None] | tuple[None, None, None]) -&gt; WordCollection\n</code></pre>Python<pre><code>__getitem__(levels: tuple[Indexer, Indexer, Indexer]) -&gt; TextBlockCollection\n</code></pre> <p>Generates OCR blocks</p> indexing is in form <p>object[para(0)], object[para(0),line(1)], object[para(0),line(1),word(2)],</p> <p>Returns:</p> Type Description <code>TextBlockCollection</code> <p>WordCollection, LineCollection, ParaCollection depending on the indexing.</p> Source code in <code>main.py</code> Python<pre><code>def __getitem__(\n    self,\n    levels: int\n    | MultiIndexer\n    | tuple[Indexer]\n    | tuple[Indexer, Indexer]\n    | tuple[Indexer, Indexer, Indexer],paragraph\n) -&gt; TextBlockCollection:\n    \"\"\"\n        Generates OCR blocks\n\n        indexing is in form:\n            object[para(0)],\n            object[para(0),line(1)],\n            object[para(0),line(1),word(2)],\n\n        Returns:\n            WordCollection, LineCollection, ParaCollection depending on the indexing.\n    \"\"\"\n    match levels:\n        case (p,) | int(p) | (slice(start=_, stop=_, step=_) as p):\n            para, line, word = p, None, None\n        case (p, l):\n            para, line, word = p, l, None\n        case (p, l, w):\n            para, line, word = p, l, w\n        case _:\n            raise IndexError(f\"Too many indexers. Expected=3, got={len(levels)}\")\n\n    para_indexer, line_indexer, word_indexer = map(\n        _get_multiindexer, (para, line, word)\n    )\n    para_ids_picked = self.para_ids[para_indexer]\n    line_ids_picked = self.line_ids[line_indexer]\n    word_ids_picked = self.word_ids[word_indexer]\n\n    fast_path_possible = _is_contiguous_slice(\n        para_indexer, line_indexer, word_indexer\n    )\n    match (para, line, word):\n        case (None, None, _):\n            return WordCollection.from_ocr(\n                self.pick_indices(self.word_ids[word_indexer])\n            )\n        case (None, _, None):\n            return LineCollection.from_ocr(\n                self.pick_indices(self.line_ids[line_indexer])\n            )\n        case (_, None, None):\n            return ParaCollection.from_ocr(\n                self.pick_indices(self.para_ids[para_indexer])\n            )\n        case (None, _, _):\n            if fast_path_possible:\n                word_parent_ids = self.parent_ids[word_ids_picked]\n                in_range_mask = ((word_parent_ids &gt;= line_ids_picked[0])\n                    &amp; (word_parent_ids &lt; line_ids_picked[-1] + 1))  # fmt: skip\n                return WordCollection(\n                    parent_ids=word_parent_ids[in_range_mask],\n                    texts=self.texts[word_ids_picked][in_range_mask],\n                    bboxes=self.bboxes[word_ids_picked][in_range_mask],\n                )\n            unique_line_ids = np.unique(line_ids_picked)\n            relevant_ids = np.flatnonzero(np.isin(self.parent_ids, unique_line_ids))\n            return WordCollection.from_ocr(\n                self.pick_indices(relevant_ids[word_indexer])\n            )\n        case (_, _, None):\n            if fast_path_possible:\n                line_parent_ids = self.parent_ids[line_ids_picked]\n                in_range_mask = ((line_parent_ids &gt;= para_ids_picked[0])\n                    &amp; (line_parent_ids &lt; para_ids_picked[-1] + 1))  # fmt: skip\n                return LineCollection(\n                    parent_ids=line_parent_ids[in_range_mask],\n                    texts=self.texts[line_ids_picked][in_range_mask],\n                    bboxes=self.bboxes[line_ids_picked][in_range_mask],\n                )\n            unique_para_ids = np.unique(para_ids_picked)\n            relevant_ids = np.flatnonzero(np.isin(self.parent_ids, unique_para_ids))\n            return LineCollection.from_ocr(\n                self.pick_indices(relevant_ids[line_indexer])\n            )\n        case _:\n            if fast_path_possible:\n                line_parent_ids = self.parent_ids[line_ids_picked]\n                relevant_line_ids = line_ids_picked[\n                    (line_parent_ids &gt;= para_ids_picked[0])\n                    &amp; (line_parent_ids &lt; para_ids_picked[-1] + 1)\n                ]\n                word_parent_ids = self.parent_ids[word_ids_picked]\n                in_range_mask = (word_parent_ids &gt;= relevant_line_ids[0]) &amp; (\n                    word_parent_ids &lt; relevant_line_ids[-1] + 1\n                )\n                return WordCollection(\n                    parent_ids=word_parent_ids[in_range_mask],\n                    texts=self.texts[word_ids_picked][in_range_mask],\n                    bboxes=self.bboxes[word_ids_picked][in_range_mask],\n                )\n            unique_para_ids = np.unique(para_ids_picked)\n            relevant_line_ids = np.flatnonzero(\n                np.isin(self.parent_ids, unique_para_ids)\n            )\n            unique_line_ids = np.unique(relevant_line_ids[line_indexer])\n            relevant_ids = np.flatnonzero(np.isin(self.parent_ids, unique_line_ids))\n            return WordCollection.from_ocr(\n                self.pick_indices(relevant_ids[word_indexer])\n            )\n</code></pre>"},{"location":"code/#main.OcrBlockCollection.__len__","title":"<code>__len__()</code>","text":"<p>Returns the number of items.</p> Source code in <code>main.py</code> Python<pre><code>def __len__(self) -&gt; int:\n    \"\"\"\n        Returns the number of items.\n    \"\"\"\n    return self.count()\n</code></pre>"},{"location":"code/#main.OcrBlockCollection.__post_init__","title":"<code>__post_init__()</code>","text":"<p>Initializes indices for paragraph , lines , words </p> <p>Splits OCR blocks into : - Paragraph ids - Line ids - word ids</p> Source code in <code>main.py</code> Python<pre><code>def __post_init__(self) -&gt; None:\n    \"\"\"\n    Initializes indices for paragraph , lines , words \n\n    Splits OCR blocks into :\n    - Paragraph ids\n    - Line ids\n    - word ids\n\n    \"\"\"\n    self.para_ids = np.flatnonzero(self.parent_ids == -1)\n    self.line_ids = np.flatnonzero(\n        (self.parent_ids &gt;= 0) &amp; (self.parent_ids &lt; self.para_ids.size)\n    )\n    self.word_ids = np.flatnonzero(\n        (self.parent_ids &gt;= self.para_ids.size)\n        &amp; (self.parent_ids &lt; (self.para_ids.size + self.line_ids.size))\n    )\n</code></pre>"},{"location":"code/#main.OcrBlockCollection.at","title":"<code>at(index)</code>","text":"<p>Returns Ocr Block at specific index. </p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>int</code> <p>Position of the element.</p> required <p>Returns:</p> Name Type Description <code>OcrBlock</code> <code>OcrBlock</code> <p>The selected Ocr Block.</p> Source code in <code>main.py</code> Python<pre><code>def at(self, index: int) -&gt; OcrBlock:\n    \"\"\"\n    Returns Ocr Block at specific index. \n\n    Args:\n        index (int): Position of the element.\n\n    Returns:\n        OcrBlock: The selected Ocr Block.\n    \"\"\"\n    return self.pick_indices([index]).item()\n</code></pre>"},{"location":"code/#main.OcrBlockCollection.count","title":"<code>count()</code>","text":"<p>Returns the calculated size of the text content.</p> Source code in <code>main.py</code> Python<pre><code>def count(self) -&gt; int:\n    \"\"\"\n        Returns the calculated size of the text content. \n    \"\"\"\n    return self.texts.size\n</code></pre>"},{"location":"code/#main.OcrBlockCollection.first","title":"<code>first()</code>","text":"<p>Returns the first OCR blcok In collection.</p> Source code in <code>main.py</code> Python<pre><code>def first(self) -&gt; OcrBlock:\n    \"\"\"\n    Returns the first OCR blcok In collection.\n    \"\"\"\n    return self.at(0)\n</code></pre>"},{"location":"code/#main.OcrBlockCollection.item","title":"<code>item()</code>","text":"<p>Returns Single OcrBlock from Collection.</p> <p>This Method checks whether the collection contains only one elemenet or not  ifmore than one is present then it raises error.  </p> <p>Returns:</p> Name Type Description <code>OcrBlock</code> <code>OcrBlock</code> <p>The Single OCR Block in the collection. </p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the collection contains more than one element.</p> Source code in <code>main.py</code> Python<pre><code>def item(self) -&gt; OcrBlock:\n    \"\"\"\n        Returns Single OcrBlock from Collection.\n\n        This Method checks whether the collection contains only one elemenet or not \n        ifmore than one is present then it raises error.  \n\n        Returns:\n            OcrBlock: The Single OCR Block in the collection. \n\n        Raises:\n            ValueError: If the collection contains more than one element.   \n    \"\"\"\n    if self.parent_ids.size != 1:\n        raise ValueError(\"Too many items in OcrResult\")\n    return OcrBlock(\n        parent_id=self.parent_ids[0],\n        text=self.texts[0],\n        bbox=self.bboxes[0],\n    )\n</code></pre>"},{"location":"code/#main.OcrBlockCollection.last","title":"<code>last()</code>","text":"<p>Returns the Last OCR blcok In collection.</p> Source code in <code>main.py</code> Python<pre><code>def last(self) -&gt; OcrBlock:\n    \"\"\"\n    Returns the Last OCR blcok In collection.\n    \"\"\"\n    return self.at(-1)\n</code></pre>"},{"location":"code/#main.OcrBlockCollection.lines_by_para","title":"<code>lines_by_para(para_indexer=None)</code>","text":"<p>Yield lines from paragraph </p> <p>Parameters:</p> Name Type Description Default <code>para_indexer</code> <code>(MultiIndexer, optional)</code> <p>index to filter paragraph. </p> <code>None</code> <p>Yields:</p> Type Description <code>LineCollection</code> <p>Collection of Lines from the paragraph.</p> Source code in <code>main.py</code> Python<pre><code>def lines_by_para(\n    self, para_indexer: MultiIndexer | None = None\n) -&gt; Iterator[LineCollection]:\n    \"\"\"\n    Yield lines from paragraph \n\n    Args:\n        para_indexer (MultiIndexer,optional): index to filter paragraph. \n\n    Yields:\n        Collection of Lines from the paragraph. \n    \"\"\"\n    match para_indexer:\n        case None:\n            yield from (self[i, slice(None)] for i in self.para_ids)\n        case _:\n            yield from (self[i, slice(None)] for i in self.para_ids[para_indexer])\n</code></pre>"},{"location":"code/#main.OcrBlockCollection.pick_indices","title":"<code>pick_indices(indexer)</code>","text":"<p>Returns OcrBlockCollection with the selected indices</p> <p>This Method filters the current collection based on the provided indexer and returns a new object with corresponding parent_id , texts and bboxes </p> <p>Parameters:</p> Name Type Description Default <code>indexer</code> <code>MultiIndexer</code> <p>Index , Slice , List , Boolean vector to select element from collections.</p> required <p>Returns:</p> Name Type Description <code>OcrBlockCollection</code> <code>OcrBlockCollection</code> <p>Collection of the filtered items. </p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If the indexer is None.</p> Source code in <code>main.py</code> Python<pre><code>def pick_indices(\n    self,\n    indexer: MultiIndexer,\n) -&gt; OcrBlockCollection:\n    \"\"\"\n        Returns OcrBlockCollection with the selected indices\n\n        This Method filters the current collection based on the provided indexer and returns a new object with corresponding parent_id , texts and bboxes \n\n        Args:\n            indexer (MultiIndexer): Index , Slice , List , Boolean vector to select element from collections.\n\n        Returns:\n            OcrBlockCollection: Collection of the filtered items. \n\n        Raises:\n            TypeError: If the indexer is None.   \n    \"\"\"\n    if indexer is None:\n        raise TypeError(f\"{type(indexer)} is not a valid indexer type\")\n    return OcrBlockCollection(\n        parent_ids=self.parent_ids[indexer],\n        texts=self.texts[indexer],\n        bboxes=self.bboxes[indexer],\n    )\n</code></pre>"},{"location":"code/#main.OcrBlockCollection.words_by_line","title":"<code>words_by_line(line_indexer=None)</code>","text":"<p>Yield words from lines.</p> <p>Parameters:</p> Name Type Description Default <code>line_indexer</code> <code>(MultiIndexer, optional)</code> <p>index to filter line. </p> <code>None</code> <p>Yields:</p> Type Description <code>WordCollection</code> <p>Collection of words from line.</p> Source code in <code>main.py</code> Python<pre><code>def words_by_line(\n    self, line_indexer: MultiIndexer | None = None\n) -&gt; Iterator[WordCollection]:\n    \"\"\"\n    Yield words from lines.\n\n    Args:\n        line_indexer (MultiIndexer,optional): index to filter line. \n\n    Yields:\n        Collection of words from line.\n    \"\"\"\n    match line_indexer:\n        case None:\n            yield from (self[i, None, slice(None)] for i in self.para_ids)\n        case _:\n            yield from (\n                self[None, i, slice(None)] for i in self.line_ids[line_indexer]\n            )\n</code></pre>"},{"location":"code/#main.OcrBlockCollection.words_by_para","title":"<code>words_by_para(para_indexer=None)</code>","text":"<p>Yield words from paragraph </p> <p>Parameters:</p> Name Type Description Default <code>para_indexer</code> <code>(MultiIndexer, optional)</code> <p>index to filter paragraph.</p> <code>None</code> <p>Yields:</p> Type Description <code>WordCollection</code> <p>Collection of words from the paragraph.</p> Source code in <code>main.py</code> Python<pre><code>def words_by_para(\n    self, para_indexer: MultiIndexer | None = None\n) -&gt; Iterator[WordCollection]:\n    \"\"\"\n    Yield words from paragraph \n\n    Args:\n        para_indexer (MultiIndexer,optional): index to filter paragraph.\n\n    Yields:\n        Collection of words from the paragraph.\n\n    \"\"\"    \n    match para_indexer:\n        case None:\n            yield from (self[i, None, slice(None)] for i in self.para_ids)\n        case _:\n            yield from (\n                self[i, None, slice(None)] for i in self.para_ids[para_indexer]\n            )\n</code></pre>"},{"location":"code/#main.ParaCollection","title":"<code>ParaCollection</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TextBlockCollection</code></p> <p>Represents a collection of paragraphs </p> <p>This class provides operations for paragraph collections, extending TextBlockCollection . It returns Para Objects for single elements  and suports indexing, slicing and aggregation at the paragraph level</p> <p>Attributes:</p> Name Type Description <code>parent_id</code> <code>IntVector</code> <p>Parent id for the text block.</p> <code>texts</code> <code>StrVector</code> <p>Array of text vector.</p> <code>bboxes</code> <code>Array2D</code> <p>Bounding Box for text element in format [x1,y1,x2,y2].</p> Source code in <code>main.py</code> Python<pre><code>class ParaCollection(TextBlockCollection):\n    \"\"\"\n    Represents a collection of paragraphs \n\n    This class provides operations for paragraph collections,\n    extending TextBlockCollection . It returns Para Objects for single elements \n    and suports indexing, slicing and aggregation at the paragraph level\n\n    Attributes:\n        parent_id (IntVector): Parent id for the text block.\n        texts (StrVector): Array of text vector.\n        bboxes (Array2D): Bounding Box for text element in format [x1,y1,x2,y2].\n    \"\"\"\n    parent_ids: IntVector\n    texts: StrVector\n    bboxes: Array2D[Shape[int, Literal[4]], np.int_]\n\n    def item(self) -&gt; Para:\n        \"\"\"\n        Return a single paragraph as a Para Object.\n\n        Returns:\n            Para: Single Paragraph element. \n\n        Raises:\n            ValueError: if collection contains more than one element.\n        \"\"\"\n        return Para(super().item())\n\n    def as_block(self) -&gt; TextBlock:\n        \"\"\"\n        Combine all paragraphs into a single TextBlock.\n\n        Merges all paragraphs texts using paragraph seperators and computes bounding box.\n\n        Returns: \n            TextBlock: Aggregated paragraph block.\n        \"\"\"\n        bbox_min = self.bboxes.min(axis=0)\n        bbox_max = self.bboxes.max(axis=0)\n        bbox = np.r_[bbox_min[:2], bbox_max[2:]]\n        return TextBlock(text=PARA_SEP.join(self.texts), bbox=bbox)\n\n    def __len__(self) -&gt; int:\n        \"\"\"\n        Returns the number of paragraphs in the collections. \n\n        Returns:\n            int: Total number of paragraphs.\n\n        \"\"\"\n        return super().count()\n\n    def count(self) -&gt; int:\n        \"\"\"\n        Returns the total nuber of paragraphs.\n\n        Returns:\n            int: Total number of paragraphs.\n        \"\"\"\n        return super().count()\n\n    def first(self) -&gt; Para:\n        \"\"\"\n        Returns the first paragraph from Collection.\n\n        Returns:\n            Para : first paragraph element.\n        \"\"\"\n        return Para(super().first())\n\n    def last(self) -&gt; Para:\n        \"\"\"\n        Returns the last paragraph from collection. \n\n        Returns:\n            Para: last paragraph element.\n        \"\"\"\n        return Para(super().last())\n\n    def at(self, index: int) -&gt; Para:\n        \"\"\"\n        Returns the paragraph at a specified index.\n\n        Args:\n            index (int): Position of the paragraph.\n\n        Returns: \n            Para: paragraph at specified index.  \n\n        \"\"\"\n        return Para(super().at(index))\n\n    @overload\n    def __getitem__(self, index: int) -&gt; Para: ...\n\n    @overload\n    def __getitem__(self, index: MultiIndexer | None) -&gt; ParaCollection: ...\n\n    def __getitem__(self, index: Indexer) -&gt; ParaCollection | Para:\n        \"\"\"\n        Retrives paragraph by indexing.\n\n        if index is int then\n            Returns: Para object.\n        else (for multiIndexer)\n            Returns: ParaCollection.\n\n        Args: \n            index (Indexer): Index or Indexing sequence.\n\n        Returns: \n            Para or ParaCollection: Based on index type. \n\n        \"\"\"\n        if isinstance(index, int):\n            return Para(\n                OcrBlock(\n                    parent_id=self.parent_ids[index],\n                    text=self.texts[index],\n                    bbox=self.bboxes[index],\n                )\n            )\n        return ParaCollection(\n            parent_ids=self.parent_ids[index],\n            texts=self.texts[index],\n            bboxes=self.bboxes[index],\n        )\n</code></pre>"},{"location":"code/#main.ParaCollection.__getitem__","title":"<code>__getitem__(index)</code>","text":"Python<pre><code>__getitem__(index: int) -&gt; Para\n</code></pre>Python<pre><code>__getitem__(index: MultiIndexer | None) -&gt; ParaCollection\n</code></pre> <p>Retrives paragraph by indexing.</p> <p>if index is int then     Returns: Para object. else (for multiIndexer)     Returns: ParaCollection.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>Indexer</code> <p>Index or Indexing sequence.</p> required <p>Returns:</p> Type Description <code>ParaCollection | Para</code> <p>Para or ParaCollection: Based on index type.</p> Source code in <code>main.py</code> Python<pre><code>def __getitem__(self, index: Indexer) -&gt; ParaCollection | Para:\n    \"\"\"\n    Retrives paragraph by indexing.\n\n    if index is int then\n        Returns: Para object.\n    else (for multiIndexer)\n        Returns: ParaCollection.\n\n    Args: \n        index (Indexer): Index or Indexing sequence.\n\n    Returns: \n        Para or ParaCollection: Based on index type. \n\n    \"\"\"\n    if isinstance(index, int):\n        return Para(\n            OcrBlock(\n                parent_id=self.parent_ids[index],\n                text=self.texts[index],\n                bbox=self.bboxes[index],\n            )\n        )\n    return ParaCollection(\n        parent_ids=self.parent_ids[index],\n        texts=self.texts[index],\n        bboxes=self.bboxes[index],\n    )\n</code></pre>"},{"location":"code/#main.ParaCollection.__len__","title":"<code>__len__()</code>","text":"<p>Returns the number of paragraphs in the collections. </p> <p>Returns:</p> Name Type Description <code>int</code> <code>int</code> <p>Total number of paragraphs.</p> Source code in <code>main.py</code> Python<pre><code>def __len__(self) -&gt; int:\n    \"\"\"\n    Returns the number of paragraphs in the collections. \n\n    Returns:\n        int: Total number of paragraphs.\n\n    \"\"\"\n    return super().count()\n</code></pre>"},{"location":"code/#main.ParaCollection.as_block","title":"<code>as_block()</code>","text":"<p>Combine all paragraphs into a single TextBlock.</p> <p>Merges all paragraphs texts using paragraph seperators and computes bounding box.</p> <p>Returns:</p> Name Type Description <code>TextBlock</code> <code>TextBlock</code> <p>Aggregated paragraph block.</p> Source code in <code>main.py</code> Python<pre><code>def as_block(self) -&gt; TextBlock:\n    \"\"\"\n    Combine all paragraphs into a single TextBlock.\n\n    Merges all paragraphs texts using paragraph seperators and computes bounding box.\n\n    Returns: \n        TextBlock: Aggregated paragraph block.\n    \"\"\"\n    bbox_min = self.bboxes.min(axis=0)\n    bbox_max = self.bboxes.max(axis=0)\n    bbox = np.r_[bbox_min[:2], bbox_max[2:]]\n    return TextBlock(text=PARA_SEP.join(self.texts), bbox=bbox)\n</code></pre>"},{"location":"code/#main.ParaCollection.at","title":"<code>at(index)</code>","text":"<p>Returns the paragraph at a specified index.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>int</code> <p>Position of the paragraph.</p> required <p>Returns:</p> Name Type Description <code>Para</code> <code>Para</code> <p>paragraph at specified index.</p> Source code in <code>main.py</code> Python<pre><code>def at(self, index: int) -&gt; Para:\n    \"\"\"\n    Returns the paragraph at a specified index.\n\n    Args:\n        index (int): Position of the paragraph.\n\n    Returns: \n        Para: paragraph at specified index.  \n\n    \"\"\"\n    return Para(super().at(index))\n</code></pre>"},{"location":"code/#main.ParaCollection.count","title":"<code>count()</code>","text":"<p>Returns the total nuber of paragraphs.</p> <p>Returns:</p> Name Type Description <code>int</code> <code>int</code> <p>Total number of paragraphs.</p> Source code in <code>main.py</code> Python<pre><code>def count(self) -&gt; int:\n    \"\"\"\n    Returns the total nuber of paragraphs.\n\n    Returns:\n        int: Total number of paragraphs.\n    \"\"\"\n    return super().count()\n</code></pre>"},{"location":"code/#main.ParaCollection.first","title":"<code>first()</code>","text":"<p>Returns the first paragraph from Collection.</p> <p>Returns:</p> Name Type Description <code>Para</code> <code>Para</code> <p>first paragraph element.</p> Source code in <code>main.py</code> Python<pre><code>def first(self) -&gt; Para:\n    \"\"\"\n    Returns the first paragraph from Collection.\n\n    Returns:\n        Para : first paragraph element.\n    \"\"\"\n    return Para(super().first())\n</code></pre>"},{"location":"code/#main.ParaCollection.item","title":"<code>item()</code>","text":"<p>Return a single paragraph as a Para Object.</p> <p>Returns:</p> Name Type Description <code>Para</code> <code>Para</code> <p>Single Paragraph element. </p> <p>Raises:</p> Type Description <code>ValueError</code> <p>if collection contains more than one element.</p> Source code in <code>main.py</code> Python<pre><code>def item(self) -&gt; Para:\n    \"\"\"\n    Return a single paragraph as a Para Object.\n\n    Returns:\n        Para: Single Paragraph element. \n\n    Raises:\n        ValueError: if collection contains more than one element.\n    \"\"\"\n    return Para(super().item())\n</code></pre>"},{"location":"code/#main.ParaCollection.last","title":"<code>last()</code>","text":"<p>Returns the last paragraph from collection. </p> <p>Returns:</p> Name Type Description <code>Para</code> <code>Para</code> <p>last paragraph element.</p> Source code in <code>main.py</code> Python<pre><code>def last(self) -&gt; Para:\n    \"\"\"\n    Returns the last paragraph from collection. \n\n    Returns:\n        Para: last paragraph element.\n    \"\"\"\n    return Para(super().last())\n</code></pre>"},{"location":"code/#main.TextBlock","title":"<code>TextBlock</code>","text":"<p>               Bases: <code>NamedTuple</code></p> <p>Represents a Text Block.</p> <p>This class stores text and its corresponding Bounding box in Text Block. </p> <p>Attributes:</p> Name Type Description <code>text</code> <code>str</code> <p>the extracted text content. </p> <code>bbox</code> <code>Vector[Shape[Literal[4]], int_]</code> <p>vector containing four coordinates of the bounding box.</p> Source code in <code>main.py</code> Python<pre><code>class TextBlock(NamedTuple):\n    \"\"\"\n        Represents a Text Block.\n\n        This class stores text and its corresponding Bounding box in Text Block. \n\n        Attributes:\n            text (str): the extracted text content. \n            bbox (Vector[Shape[Literal[4]], np.int_]): vector containing four coordinates of the bounding box. \n\n    \"\"\"\n    text: str\n    bbox: Vector[Shape[Literal[4]], np.int_]\n</code></pre>"},{"location":"code/#main.TextBlockCollection","title":"<code>TextBlockCollection</code>  <code>dataclass</code>","text":"<p>               Bases: <code>Sized</code>, <code>ABC</code></p> <p>Abstract Dataclass representing a collection of text blocks. This class defines an interface for the collection of OCR text elements (paragraph , lines , words). It stores text data amd bounding boxes.</p> <p>Attributes:</p> Name Type Description <code>parent_id</code> <code>IntVector</code> <p>Parent id for the text block.</p> <code>texts</code> <code>StrVector</code> <p>Array of text vector.</p> <code>bboxes</code> <code>Array2D</code> <p>Bounding Box for text element in format [x1,y1,x2,y2].</p> <p>This is the abstract class , so the subclasses must implement indexing and colection-specific behavior.</p> Source code in <code>main.py</code> Python<pre><code>@dataclass\nclass TextBlockCollection(Sized, ABC):\n    \"\"\"\n    Abstract Dataclass representing a collection of text blocks.\n    This class defines an interface for the collection of OCR text elements (paragraph , lines , words).\n    It stores text data amd bounding boxes.\n\n\n    Attributes: \n        parent_id (IntVector): Parent id for the text block.\n        texts (StrVector): Array of text vector.\n        bboxes (Array2D): Bounding Box for text element in format [x1,y1,x2,y2].\n\n    This is the abstract class , so the subclasses must implement indexing and colection-specific behavior.\n\n    \"\"\"\n    parent_ids: IntVector\n    texts: StrVector\n    bboxes: Array2D[Shape[int, Literal[4]], np.int_]\n\n    @abstractmethod\n    def item(self) -&gt; OcrBlock:\n        \"\"\"\n        Returns a single OcrBlock from collection. \n\n        This method expects  the collection to contain exactly one element. \n        Error is raised if there is more than one element.\n\n        Returns: \n            OcrBlock: Single OCR block. \n\n        Raises:\n            ValueError: if collection contains more than one element. \n        \"\"\"\n        if self.texts.size != 1:\n            raise ValueError(\"Too many items in OcrResult\")\n        return OcrBlock(\n            parent_id=self.parent_ids[0],\n            text=self.texts[0],\n            bbox=self.bboxes[0],\n        )\n\n    @abstractmethod\n    def as_block(self) -&gt; TextBlock:\n        \"\"\"\n        Combine multiple text elements into single TextBlock. \n\n        This method merges all text entries into a single string seperated by paragraph seperators and calculates the bounding box for each text element.\n\n        Returns:\n            TextBlock: Aggregated text block with combined bounding box. \n\n        \"\"\"\n        bbox_min = self.bboxes.min(axis=0)\n        bbox_max = self.bboxes.max(axis=0)\n        bbox = np.r_[bbox_min[:2], bbox_max[2:]]\n        return TextBlock(text=PARA_SEP.join(self.texts), bbox=bbox)\n\n    @abstractmethod\n    def __len__(self) -&gt; int:\n        \"\"\"\n        Returns the number of elements in that collection. \n        Returns:\n            int: Total number of text elements. \n        \"\"\"\n        return self.count()\n\n    @abstractmethod\n    def count(self) -&gt; int:\n        \"\"\"\n        Returns the total number of text element in the collections.\n\n        Returns: \n            int: Size of the text elemens in the collections. \n        \"\"\"\n        return self.texts.size\n\n    @abstractmethod\n    def first(self) -&gt; OcrBlock:\n        \"\"\"\n        Returns the First OcrBlock from the collections.\n\n        Returns:                \n            OcrBlock: First Element. \n        \"\"\"\n        return self.at(0)\n\n    @abstractmethod\n    def last(self) -&gt; OcrBlock:\n        \"\"\"\n        Returns the last OcrBlock from the collections.\n\n        Returns:                \n            OcrBlock: Last Element. \n        \"\"\"\n        return self.at(-1)\n\n    @abstractmethod\n    def at(self, index: int) -&gt; OcrBlock:\n        \"\"\"\n        Return the Ocr Block at a specific index. \n\n        Args:\n            index (int): index for the OcrBlock.\n\n        Returns:\n            OcrBlock: Selected element.\n        \"\"\"\n        return self[index]\n\n    @overload\n    @abstractmethod\n    def __getitem__(self, index: int) -&gt; OcrBlock: ...\n\n    @overload\n    @abstractmethod\n    def __getitem__(self, index: MultiIndexer | None) -&gt; TextBlockCollection: ...\n\n    @abstractmethod\n    def __getitem__(self, index: Indexer) -&gt; TextBlockCollection | OcrBlock: ...\n\n    @classmethod\n    def from_ocr(cls, ocr_collection: OcrBlockCollection) -&gt; Self:\n        \"\"\"\n        Creates the Text Collectionblock from the ocr block collection. \n\n        Args:\n            ocr_collection (OcrBlockCollection): Source OCR data.\n\n        Returns:\n            TextBlockCollection: New Collection with copeid data.\n        \"\"\"\n        return cls(\n            parent_ids=ocr_collection.parent_ids,\n            texts=ocr_collection.texts,\n            bboxes=ocr_collection.bboxes,\n        )\n</code></pre>"},{"location":"code/#main.TextBlockCollection.__len__","title":"<code>__len__()</code>  <code>abstractmethod</code>","text":"<p>Returns the number of elements in that collection.  Returns:     int: Total number of text elements.</p> Source code in <code>main.py</code> Python<pre><code>@abstractmethod\ndef __len__(self) -&gt; int:\n    \"\"\"\n    Returns the number of elements in that collection. \n    Returns:\n        int: Total number of text elements. \n    \"\"\"\n    return self.count()\n</code></pre>"},{"location":"code/#main.TextBlockCollection.as_block","title":"<code>as_block()</code>  <code>abstractmethod</code>","text":"<p>Combine multiple text elements into single TextBlock. </p> <p>This method merges all text entries into a single string seperated by paragraph seperators and calculates the bounding box for each text element.</p> <p>Returns:</p> Name Type Description <code>TextBlock</code> <code>TextBlock</code> <p>Aggregated text block with combined bounding box.</p> Source code in <code>main.py</code> Python<pre><code>@abstractmethod\ndef as_block(self) -&gt; TextBlock:\n    \"\"\"\n    Combine multiple text elements into single TextBlock. \n\n    This method merges all text entries into a single string seperated by paragraph seperators and calculates the bounding box for each text element.\n\n    Returns:\n        TextBlock: Aggregated text block with combined bounding box. \n\n    \"\"\"\n    bbox_min = self.bboxes.min(axis=0)\n    bbox_max = self.bboxes.max(axis=0)\n    bbox = np.r_[bbox_min[:2], bbox_max[2:]]\n    return TextBlock(text=PARA_SEP.join(self.texts), bbox=bbox)\n</code></pre>"},{"location":"code/#main.TextBlockCollection.at","title":"<code>at(index)</code>  <code>abstractmethod</code>","text":"<p>Return the Ocr Block at a specific index. </p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>int</code> <p>index for the OcrBlock.</p> required <p>Returns:</p> Name Type Description <code>OcrBlock</code> <code>OcrBlock</code> <p>Selected element.</p> Source code in <code>main.py</code> Python<pre><code>@abstractmethod\ndef at(self, index: int) -&gt; OcrBlock:\n    \"\"\"\n    Return the Ocr Block at a specific index. \n\n    Args:\n        index (int): index for the OcrBlock.\n\n    Returns:\n        OcrBlock: Selected element.\n    \"\"\"\n    return self[index]\n</code></pre>"},{"location":"code/#main.TextBlockCollection.count","title":"<code>count()</code>  <code>abstractmethod</code>","text":"<p>Returns the total number of text element in the collections.</p> <p>Returns:</p> Name Type Description <code>int</code> <code>int</code> <p>Size of the text elemens in the collections.</p> Source code in <code>main.py</code> Python<pre><code>@abstractmethod\ndef count(self) -&gt; int:\n    \"\"\"\n    Returns the total number of text element in the collections.\n\n    Returns: \n        int: Size of the text elemens in the collections. \n    \"\"\"\n    return self.texts.size\n</code></pre>"},{"location":"code/#main.TextBlockCollection.first","title":"<code>first()</code>  <code>abstractmethod</code>","text":"<p>Returns the First OcrBlock from the collections.</p> <p>Returns:</p> Name Type Description <code>OcrBlock</code> <code>OcrBlock</code> <p>First Element.</p> Source code in <code>main.py</code> Python<pre><code>@abstractmethod\ndef first(self) -&gt; OcrBlock:\n    \"\"\"\n    Returns the First OcrBlock from the collections.\n\n    Returns:                \n        OcrBlock: First Element. \n    \"\"\"\n    return self.at(0)\n</code></pre>"},{"location":"code/#main.TextBlockCollection.from_ocr","title":"<code>from_ocr(ocr_collection)</code>  <code>classmethod</code>","text":"<p>Creates the Text Collectionblock from the ocr block collection. </p> <p>Parameters:</p> Name Type Description Default <code>ocr_collection</code> <code>OcrBlockCollection</code> <p>Source OCR data.</p> required <p>Returns:</p> Name Type Description <code>TextBlockCollection</code> <code>Self</code> <p>New Collection with copeid data.</p> Source code in <code>main.py</code> Python<pre><code>@classmethod\ndef from_ocr(cls, ocr_collection: OcrBlockCollection) -&gt; Self:\n    \"\"\"\n    Creates the Text Collectionblock from the ocr block collection. \n\n    Args:\n        ocr_collection (OcrBlockCollection): Source OCR data.\n\n    Returns:\n        TextBlockCollection: New Collection with copeid data.\n    \"\"\"\n    return cls(\n        parent_ids=ocr_collection.parent_ids,\n        texts=ocr_collection.texts,\n        bboxes=ocr_collection.bboxes,\n    )\n</code></pre>"},{"location":"code/#main.TextBlockCollection.item","title":"<code>item()</code>  <code>abstractmethod</code>","text":"<p>Returns a single OcrBlock from collection. </p> <p>This method expects  the collection to contain exactly one element.  Error is raised if there is more than one element.</p> <p>Returns:</p> Name Type Description <code>OcrBlock</code> <code>OcrBlock</code> <p>Single OCR block. </p> <p>Raises:</p> Type Description <code>ValueError</code> <p>if collection contains more than one element.</p> Source code in <code>main.py</code> Python<pre><code>@abstractmethod\ndef item(self) -&gt; OcrBlock:\n    \"\"\"\n    Returns a single OcrBlock from collection. \n\n    This method expects  the collection to contain exactly one element. \n    Error is raised if there is more than one element.\n\n    Returns: \n        OcrBlock: Single OCR block. \n\n    Raises:\n        ValueError: if collection contains more than one element. \n    \"\"\"\n    if self.texts.size != 1:\n        raise ValueError(\"Too many items in OcrResult\")\n    return OcrBlock(\n        parent_id=self.parent_ids[0],\n        text=self.texts[0],\n        bbox=self.bboxes[0],\n    )\n</code></pre>"},{"location":"code/#main.TextBlockCollection.last","title":"<code>last()</code>  <code>abstractmethod</code>","text":"<p>Returns the last OcrBlock from the collections.</p> <p>Returns:</p> Name Type Description <code>OcrBlock</code> <code>OcrBlock</code> <p>Last Element.</p> Source code in <code>main.py</code> Python<pre><code>@abstractmethod\ndef last(self) -&gt; OcrBlock:\n    \"\"\"\n    Returns the last OcrBlock from the collections.\n\n    Returns:                \n        OcrBlock: Last Element. \n    \"\"\"\n    return self.at(-1)\n</code></pre>"},{"location":"code/#main.WordCollection","title":"<code>WordCollection</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TextBlockCollection</code></p> <p>Represents a collection of words. </p> <p>This class provides operations for word collections, extending TextBlockCollection . It returns Word Objects for single elements  and suports indexing, slicing and aggregation at the word level</p> <p>Attributes:</p> Name Type Description <code>parent_id</code> <code>IntVector</code> <p>Parent id for the text block.</p> <code>texts</code> <code>StrVector</code> <p>Array of text vector.</p> <code>bboxes</code> <code>Array2D</code> <p>Bounding Box for text element in format [x1,y1,x2,y2].</p> Source code in <code>main.py</code> Python<pre><code>class WordCollection(TextBlockCollection):\n    \"\"\"\n    Represents a collection of words. \n\n    This class provides operations for word collections,\n    extending TextBlockCollection . It returns Word Objects for single elements \n    and suports indexing, slicing and aggregation at the word level\n\n    Attributes:\n        parent_id (IntVector): Parent id for the text block.\n        texts (StrVector): Array of text vector.\n        bboxes (Array2D): Bounding Box for text element in format [x1,y1,x2,y2].\n    \"\"\"\n    parent_ids: IntVector\n    texts: StrVector\n    bboxes: Array2D[Shape[int, Literal[4]], np.int_]\n\n    def item(self) -&gt; Word:\n        \"\"\"\n        Return a single word as a Word Object.\n\n        Returns:\n            Line: Single Word element. \n\n        Raises:\n            ValueError: if collection contains more than one element.\n        \"\"\"\n        return Word(super().item())\n\n    def as_block(self) -&gt; TextBlock:\n        \"\"\"\n        Combine all words into a single TextBlock.\n\n        Merges all words text using word seperators along with  bounding box computations.\n\n        Returns: \n            TextBlock: Aggregated word block.\n        \"\"\"\n        bbox_min = self.bboxes.min(axis=0)\n        bbox_max = self.bboxes.max(axis=0)\n        bbox = np.r_[bbox_min[:2], bbox_max[2:]]\n        return TextBlock(text=WORD_SEP.join(self.texts), bbox=bbox)\n\n    def __len__(self) -&gt; int:\n        \"\"\"\n        Returns the number of words in the collections.\n\n        Returns:\n            int: Total number of words.\n\n        \"\"\"\n        return len(super())\n\n    def count(self) -&gt; int:\n        \"\"\"\n        Returns the total nuber of words.\n\n        Returns:\n            int: Total number of words.\n        \"\"\"\n        return super().count()\n\n    def first(self) -&gt; Word:\n        \"\"\"\n        Returns the first word from Collection.\n\n        Returns:\n            Word: first word element.\n        \"\"\"\n        return Word(super().first())\n\n    def last(self) -&gt; Word:\n        \"\"\"\n        Returns the last word from Collection\n\n        Returns:\n            Word: last word element.\n        \"\"\"\n        return Word(super().last())\n\n    def at(self, index: int) -&gt; Word:\n        \"\"\"\n        Returns the word at a specified index.\n\n        Args:\n            index (int): Position of the word.\n\n        Returns: \n            Word: word at specified index.  \n\n        \"\"\"\n        return Word(super().at(index))\n\n    @overload\n    def __getitem__(self, index: int) -&gt; Word: ...\n\n    @overload\n    def __getitem__(self, index: MultiIndexer | None) -&gt; WordCollection: ...\n\n    def __getitem__(self, index: Indexer) -&gt; WordCollection | Word:\n        \"\"\"\n        Retrives word by indexing\n\n        if index is int  then\n            Returns: word object.\n        else (for multiIndexer)\n            Returns: WordCollection.\n\n        Args: \n            index (Indexer): Index or Indexing sequence.\n\n        Returns: \n            Word or WordCollection: Based on index type. \n        \"\"\"\n        if isinstance(index, int):\n            return Word(\n                OcrBlock(\n                    parent_id=self.parent_ids[index],\n                    text=self.texts[index],\n                    bbox=self.bboxes[index],\n                )\n            )\n        return WordCollection(\n            parent_ids=self.parent_ids[index],\n            texts=self.texts[index],\n            bboxes=self.bboxes[index],\n        )\n</code></pre>"},{"location":"code/#main.WordCollection.__getitem__","title":"<code>__getitem__(index)</code>","text":"Python<pre><code>__getitem__(index: int) -&gt; Word\n</code></pre>Python<pre><code>__getitem__(index: MultiIndexer | None) -&gt; WordCollection\n</code></pre> <p>Retrives word by indexing</p> <p>if index is int  then     Returns: word object. else (for multiIndexer)     Returns: WordCollection.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>Indexer</code> <p>Index or Indexing sequence.</p> required <p>Returns:</p> Type Description <code>WordCollection | Word</code> <p>Word or WordCollection: Based on index type.</p> Source code in <code>main.py</code> Python<pre><code>def __getitem__(self, index: Indexer) -&gt; WordCollection | Word:\n    \"\"\"\n    Retrives word by indexing\n\n    if index is int  then\n        Returns: word object.\n    else (for multiIndexer)\n        Returns: WordCollection.\n\n    Args: \n        index (Indexer): Index or Indexing sequence.\n\n    Returns: \n        Word or WordCollection: Based on index type. \n    \"\"\"\n    if isinstance(index, int):\n        return Word(\n            OcrBlock(\n                parent_id=self.parent_ids[index],\n                text=self.texts[index],\n                bbox=self.bboxes[index],\n            )\n        )\n    return WordCollection(\n        parent_ids=self.parent_ids[index],\n        texts=self.texts[index],\n        bboxes=self.bboxes[index],\n    )\n</code></pre>"},{"location":"code/#main.WordCollection.__len__","title":"<code>__len__()</code>","text":"<p>Returns the number of words in the collections.</p> <p>Returns:</p> Name Type Description <code>int</code> <code>int</code> <p>Total number of words.</p> Source code in <code>main.py</code> Python<pre><code>def __len__(self) -&gt; int:\n    \"\"\"\n    Returns the number of words in the collections.\n\n    Returns:\n        int: Total number of words.\n\n    \"\"\"\n    return len(super())\n</code></pre>"},{"location":"code/#main.WordCollection.as_block","title":"<code>as_block()</code>","text":"<p>Combine all words into a single TextBlock.</p> <p>Merges all words text using word seperators along with  bounding box computations.</p> <p>Returns:</p> Name Type Description <code>TextBlock</code> <code>TextBlock</code> <p>Aggregated word block.</p> Source code in <code>main.py</code> Python<pre><code>def as_block(self) -&gt; TextBlock:\n    \"\"\"\n    Combine all words into a single TextBlock.\n\n    Merges all words text using word seperators along with  bounding box computations.\n\n    Returns: \n        TextBlock: Aggregated word block.\n    \"\"\"\n    bbox_min = self.bboxes.min(axis=0)\n    bbox_max = self.bboxes.max(axis=0)\n    bbox = np.r_[bbox_min[:2], bbox_max[2:]]\n    return TextBlock(text=WORD_SEP.join(self.texts), bbox=bbox)\n</code></pre>"},{"location":"code/#main.WordCollection.at","title":"<code>at(index)</code>","text":"<p>Returns the word at a specified index.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>int</code> <p>Position of the word.</p> required <p>Returns:</p> Name Type Description <code>Word</code> <code>Word</code> <p>word at specified index.</p> Source code in <code>main.py</code> Python<pre><code>def at(self, index: int) -&gt; Word:\n    \"\"\"\n    Returns the word at a specified index.\n\n    Args:\n        index (int): Position of the word.\n\n    Returns: \n        Word: word at specified index.  \n\n    \"\"\"\n    return Word(super().at(index))\n</code></pre>"},{"location":"code/#main.WordCollection.count","title":"<code>count()</code>","text":"<p>Returns the total nuber of words.</p> <p>Returns:</p> Name Type Description <code>int</code> <code>int</code> <p>Total number of words.</p> Source code in <code>main.py</code> Python<pre><code>def count(self) -&gt; int:\n    \"\"\"\n    Returns the total nuber of words.\n\n    Returns:\n        int: Total number of words.\n    \"\"\"\n    return super().count()\n</code></pre>"},{"location":"code/#main.WordCollection.first","title":"<code>first()</code>","text":"<p>Returns the first word from Collection.</p> <p>Returns:</p> Name Type Description <code>Word</code> <code>Word</code> <p>first word element.</p> Source code in <code>main.py</code> Python<pre><code>def first(self) -&gt; Word:\n    \"\"\"\n    Returns the first word from Collection.\n\n    Returns:\n        Word: first word element.\n    \"\"\"\n    return Word(super().first())\n</code></pre>"},{"location":"code/#main.WordCollection.item","title":"<code>item()</code>","text":"<p>Return a single word as a Word Object.</p> <p>Returns:</p> Name Type Description <code>Line</code> <code>Word</code> <p>Single Word element. </p> <p>Raises:</p> Type Description <code>ValueError</code> <p>if collection contains more than one element.</p> Source code in <code>main.py</code> Python<pre><code>def item(self) -&gt; Word:\n    \"\"\"\n    Return a single word as a Word Object.\n\n    Returns:\n        Line: Single Word element. \n\n    Raises:\n        ValueError: if collection contains more than one element.\n    \"\"\"\n    return Word(super().item())\n</code></pre>"},{"location":"code/#main.WordCollection.last","title":"<code>last()</code>","text":"<p>Returns the last word from Collection</p> <p>Returns:</p> Name Type Description <code>Word</code> <code>Word</code> <p>last word element.</p> Source code in <code>main.py</code> Python<pre><code>def last(self) -&gt; Word:\n    \"\"\"\n    Returns the last word from Collection\n\n    Returns:\n        Word: last word element.\n    \"\"\"\n    return Word(super().last())\n</code></pre>"},{"location":"code/#main.main","title":"<code>main()</code>","text":"<p>Hello from documentation-tutorial!</p> Source code in <code>main.py</code> Python<pre><code>def main():\n    \"\"\"\n    Hello from documentation-tutorial!\n    \"\"\"\n    print(\"Hello from documentation-tutorial!\")\n</code></pre>"},{"location":"api/","title":"Index","text":"<p>Task Manager API</p>"},{"location":"api/#task-manager-api","title":"Task Manager API","text":""},{"location":"api/#classes","title":"Classes","text":"<ul> <li>TaskManager</li> </ul>"},{"location":"api/#interfaces","title":"Interfaces","text":"<ul> <li>Task</li> </ul>"},{"location":"api/classes/TaskManager/","title":"TaskManager","text":"<p>Task Manager API</p> <p>Task Manager API / TaskManager</p>"},{"location":"api/classes/TaskManager/#class-taskmanager","title":"Class: TaskManager","text":"<p>Defined in: taskManager.ts:6</p> <p>Handles all task operations.</p>"},{"location":"api/classes/TaskManager/#constructors","title":"Constructors","text":""},{"location":"api/classes/TaskManager/#constructor","title":"Constructor","text":"<p>new TaskManager(): <code>TaskManager</code></p>"},{"location":"api/classes/TaskManager/#returns","title":"Returns","text":"<p><code>TaskManager</code></p>"},{"location":"api/classes/TaskManager/#methods","title":"Methods","text":""},{"location":"api/classes/TaskManager/#addtask","title":"addTask()","text":"<p>addTask(<code>title</code>): <code>void</code></p> <p>Defined in: taskManager.ts:18</p> <p>Adds a new task.</p>"},{"location":"api/classes/TaskManager/#parameters","title":"Parameters","text":""},{"location":"api/classes/TaskManager/#title","title":"title","text":"<p><code>string</code></p> <p>Title of task</p>"},{"location":"api/classes/TaskManager/#returns_1","title":"Returns","text":"<p><code>void</code></p>"},{"location":"api/classes/TaskManager/#completetask","title":"completeTask()","text":"<p>completeTask(<code>id</code>): <code>void</code></p> <p>Defined in: taskManager.ts:53</p> <p>Marks a task as completed.</p>"},{"location":"api/classes/TaskManager/#parameters_1","title":"Parameters","text":""},{"location":"api/classes/TaskManager/#id","title":"id","text":"<p><code>number</code></p> <p>Task ID</p>"},{"location":"api/classes/TaskManager/#returns_2","title":"Returns","text":"<p><code>void</code></p>"},{"location":"api/classes/TaskManager/#deletetask","title":"deleteTask()","text":"<p>deleteTask(<code>id</code>): <code>void</code></p> <p>Defined in: taskManager.ts:68</p> <p>Deletes a task.</p>"},{"location":"api/classes/TaskManager/#parameters_2","title":"Parameters","text":""},{"location":"api/classes/TaskManager/#id_1","title":"id","text":"<p><code>number</code></p> <p>Task ID</p>"},{"location":"api/classes/TaskManager/#returns_3","title":"Returns","text":"<p><code>void</code></p>"},{"location":"api/classes/TaskManager/#viewtasks","title":"viewTasks()","text":"<p>viewTasks(): <code>void</code></p> <p>Defined in: taskManager.ts:34</p> <p>Displays all tasks.</p>"},{"location":"api/classes/TaskManager/#returns_4","title":"Returns","text":"<p><code>void</code></p>"},{"location":"api/interfaces/Task/","title":"Task","text":"<p>Task Manager API</p> <p>Task Manager API / Task</p>"},{"location":"api/interfaces/Task/#interface-task","title":"Interface: Task","text":"<p>Defined in: task.ts:4</p> <p>Represents a single task.</p>"},{"location":"api/interfaces/Task/#properties","title":"Properties","text":""},{"location":"api/interfaces/Task/#completed","title":"completed","text":"<p>completed: <code>boolean</code></p> <p>Defined in: task.ts:19</p> <p>Completion status</p>"},{"location":"api/interfaces/Task/#id","title":"id","text":"<p>id: <code>number</code></p> <p>Defined in: task.ts:9</p> <p>Unique ID of task</p>"},{"location":"api/interfaces/Task/#title","title":"title","text":"<p>title: <code>string</code></p> <p>Defined in: task.ts:14</p> <p>Title of task</p>"},{"location":"schema/openapi/","title":"JSON Schema","text":""}]}